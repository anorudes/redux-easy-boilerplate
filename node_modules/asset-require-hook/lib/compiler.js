'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = compiler;

var _fs = require('fs');

var _loaderUtils = require('loader-utils');

var _lodash = require('lodash.assign');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function compiler(name, options) {
  return function compile(file) {
    var content = (0, _fs.readFileSync)(file);

    if (options.limit !== undefined) {
      var limit = parseInt(options.limit || 0, 10);
      if (limit <= 0 || content.length < limit) {
        var mime = options.mimetype || require('mime').lookup(file);
        return 'data:' + (mime ? mime + ';' : '') + 'base64,' + content.toString('base64');
      }
    }

    var context = { resourcePath: file };
    return (0, _loaderUtils.interpolateName)(context, name, (0, _lodash2.default)({}, options, {
      content: content
    }));
  };
}
module.exports = exports['default'];